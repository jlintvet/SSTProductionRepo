# CLAUDE.md — Moved

This file used to hold the SST Fishing Map project's process rules directly in
the repo. As of 2026-08-18, that content has moved to Claude's project memory
for the "SST Development" Cowork project (not tracked in this repo), at Jon's
request — a rules file living only in git was easy for a session to never
open, since nothing in Claude's session automatically surfaces it. Project
memory *is* surfaced automatically at the start of every session.

**If you are Claude working in this repo:** read the project memory file
`project_claude_process_rules.md` in full (via `project_memory_read`) before
any code, git, or deploy action. It covers the Dropbox-truncation rules, the
`/tmp` clone git workflow, GitHub token handling, Vercel deployment
verification, the reference-docs table, NOAA marine forecast zone tables,
design rules, architecture quick reference, and community feature rules that
previously lived in this file.

**If you are a human (or a different tool) reading this file:** ask Jon for
the current contents of that project memory file, or check the SST
Development project's memory index (`MEMORY.md`) in Claude's Cowork project
memory.
